<nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}">
      <img src="{{ asset('assets/img/Logo.png') }}" alt="Logo" id="logo" height="50">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
      aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link" href="{{ path('home') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('cart_index') }}">Panier</a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto">
        {% if app.user %}
        <li class="nav-item dropstart">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true"
            aria-expanded="false">Bonjour {{ app.user.firstname }} !</a>
          <div class="dropdown-menu bg-light">
            {% if 'ROLE_ADMIN' in app.user.roles or 'ROLE_EMPLOYEE' in app.user.roles %}
            <a class="dropdown-item" href="{{ path('admin') }}">Système d'administration</a>
            {% endif %}
            <a class="dropdown-item"
              href="{{ path('user_space', {id:app.user.id, firstname: app.user.firstname, lastname: app.user.lastname}) }}">Espace
              utilisateur</a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="{{ path('logout') }}">Déconnexion</a>
          </div>
        </li>
        {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('login') }}">Connexion</a>
        </li>
      </ul>
      {% endif %}
    </div>
  </div>
</nav>